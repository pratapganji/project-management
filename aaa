SELECT ROLE_NAME
FROM A167969NURAREC.FID_ROLE
WHERE FID_NAME = 'OLYMPUS_FID';


SELECT ROLE_NAME,
       TABLE_NAME,
       PERMISSION
FROM A167969NURAREC.ROLE_TABLE_PERMISSION
WHERE ROLE_NAME IN (
    SELECT ROLE_NAME
    FROM A167969NURAREC.FID_ROLE
    WHERE FID_NAME = 'OLYMPUS_FID'
);
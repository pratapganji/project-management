String profile = System.getenv("PROFILE");              // OCP/container way
if (profile == null || profile.isBlank()) {
    profile = System.getProperty("spring.profiles.active");   // Maven/Java -D way
}
if (profile == null || profile.isBlank()) {
    profile = "dev";                                    // safe fallback
}
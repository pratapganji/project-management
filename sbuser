
SELECT COUNT(*)
FROM C167969RPC.OM_CATALOGUE_USER_CONSUMPTION
WHERE TRUNC(DWH_CREATED_TIMESTAMP) = DATE '2026-04-13'
  AND USAGE_METHOD = 'JDBC API';


SELECT *
FROM C167969RPC.OM_CATALOGUE_USER_CONSUMPTION
WHERE DWH_BUSINESS_DATE BETWEEN DATE '2026-04-06' AND DATE '2026-04-12'
  AND USAGE_METHOD = 'JDBC API'
FETCH FIRST 20 ROWS ONLY;



SELECT COUNT(*)
FROM C167969RPC.OM_CATALOGUE_USER_CONSUMPTION
WHERE DWH_BUSINESS_DATE BETWEEN DATE '2026-04-06' AND DATE '2026-04-12'
  AND USAGE_METHOD = 'JDBC API';



SELECT COUNT(*)
FROM C167969RPC.OM_CATALOGUE_USER_CONSUMPTION
WHERE DWH_BUSINESS_DATE BETWEEN DATE '2026-04-06' AND DATE '2026-04-12';



SB User Consumption – Automation Testing Details

🔹 Job / Process Details
Job Name:
restart-olympus-sb-user-consumption-pod

Namespace / Project:
icg-isg-olympus-data-176352

Trigger Type:
OpenShift CronJob

Schedule:
0 2 * * 1 (Runs every Monday at 2:00 AM)

---

🔹 Source / Input Details

Source System:
STARBURST

Source Tables:
1. gfolydal_managed.jdbc_centralized_audit_log
2. gfolydal_managed.jdbc_centralized_audit_common
3. gfolydal_managed.jdbc_centralized_audit_log (used multiple times in joins)

Date Range Read:
Last 7 days data  
Example: 2026-04-06 to 2026-04-12

API / Usage Type:
JDBC

Source Query Fetches:
- log.client_id AS USER_ID
- log.status
- st.start_time, et.end_time
- log.query_text AS request_json
- comm.datasource_type AS application_name
- 'JDBC API' AS API

Authentication / Dependencies:
- NGC secrets used for credential retrieval
- Oracle DB connection established using retrieved credentials
- Truststore file used: config/uat/UAT_cacerts.jks

---

🔹 Target / Load Details

Target Table:
OM_CATALOGUE_USER_CONSUMPTION

Data Source Confirmation:
Query executed from data source: STARBURST

Data Fetch Summary:
- Batch 1 size: 50000 (Cumulative: 50000)
- Batch 2 size: 50000 (Cumulative: 100000)
- Batch 3 size: 9681 (Cumulative: 109681)
- Server sent all rows successfully

Total Rows Read From Source:
109681

Total Error Rows:
0

Load Process:
- Data fetched from API logs
- Tables metadata fetched
- Data loaded into target table OM_CATALOGUE_USER_CONSUMPTION

File Handling:
- Staging file generated:
  /home/eqcdrpt/citidigital/cd_user_consumption/autosys_logs/staging_user_consumption_20260413020102527256.csv
- File removed after successful load

Execution Time:
625 minutes 27 seconds

Final Status:
Process completed successfully




Job / Process Details – SB User Consumption

Job Name:
restart-olympus-sb-user-consumption-pod

Namespace / Project:
icg-isg-olympus-data-176352

Trigger Type:
OpenShift CronJob

Schedule:
0 2 * * 1 (Runs every Monday at 2:00 AM)

Process Flow:
1. CronJob triggers pod restart for SB user consumption
2. Existing pod is deleted
3. New pod is created
4. On startup, user consumption process is triggered
5. Reads source data (last 7 days)
6. Cleans target table (OM_CATALOGUE_USER_CONSUMPTION)
7. Loads fresh data into target table
8. Process completes successfully

Sample Logs:
- Deleting the pod olympus-sb-user-consumption
- Waiting for the pod to be recreated
- Pod restart completed successfully

Data Behavior:
- Weekly job
- Processes last 7 days data
- Refreshes (cleans and reloads) target table data

Execution Flow:
CronJob → Pod Restart → Application Startup → Python Job Execution → Data Load







SELECT column_name,
       data_type,
       data_length
FROM all_tab_columns
WHERE owner = 'C167969RPC'
  AND table_name = 'OM_CATALOGUE_USER_CONSUMPTION'
  AND column_name = 'TABLE_PHYSICAL_NAME';


SELECT column_name,
       data_type,
       data_length
FROM all_tab_columns
WHERE owner = 'C167969RPC'
  AND table_name = 'OM_CATALOGUE_USER_CONSUMPTION'
ORDER BY column_id;


UAT Issue – ORA-12899 on OM_CATALOGUE_USER_CONSUMPTION (TABLE_PHYSICAL_NAME length)


Hi Omkar,

In UAT, we are again encountering the below error during the SB User Consumption load:

ORA-12899: value too large for column  
C167969RPC.OM_CATALOGUE_USER_CONSUMPTION.TABLE_PHYSICAL_NAME  
(actual: 53, maximum: 50)

Earlier, we had observed the same issue and as suggested, we reran the process, after which the job completed successfully. However, we are now seeing this error again.

To further analyze, I added logging before the insert step and identified the exact table name causing the issue:

TABLE NAME TOO LONG: OM_RISK_FRTB_SENSITIVITY_COMPLETENESS_RESULT_DATE_MAP  
Length: 53

This confirms that the failure is due to incoming source data exceeding the current column size limit (50), rather than an issue with the migration process.

Since we are currently in UAT and the job has already been handed over for testing, could you please advise on how to proceed further?

Should we consider increasing the column size for TABLE_PHYSICAL_NAME as a permanent fix, or handle this via trimming at the application level?

Please let me know your suggestion.

Thanks,  
Pratap




for line in csv_reader:
    table_name_val = line[3]

    if table_name_val and len(table_name_val) > 50:
        print("TABLE NAME TOO LONG:", table_name_val, "length:", len(table_name_val))

    data.append(tuple(line))



SELECT *
FROM C167969RPC.OM_CATALOGUE_USER_CONSUMPTION
ORDER BY DWH_CREATED_TIMESTAMP DESC
FETCH FIRST 20 ROWS ONLY;


SELECT *
FROM C167969RPC.OM_CATALOGUE_USER_CONSUMPTION
ORDER BY DWH_CREATED_TIMESTAMP DESC
FETCH FIRST 20 ROWS ONLY;


SELECT *
FROM C167969RPC.OM_CATALOGUE_USER_CONSUMPTION
WHERE TRUNC(DWH_CREATED_TIMESTAMP) = TRUNC(SYSDATE)
FETCH FIRST 20 ROWS ONLY;

SELECT COUNT(*)
FROM C167969RPC.OM_CATALOGUE_USER_CONSUMPTION
WHERE TRUNC(DWH_CREATED_TIMESTAMP) = TRUNC(SYSDATE);


SELECT *
FROM C167969RPC.OM_CATALOGUE_USER_CONSUMPTION
WHERE USAGE_METHOD = 'JDBC API'
AND TRUNC(DWH_CREATED_TIMESTAMP) = TRUNC(SYSDATE)
FETCH FIRST 20 ROWS ONLY;


SELECT TABLE_PHYSICAL_NAME, LENGTH(TABLE_PHYSICAL_NAME)
FROM C167969RPC.OM_CATALOGUE_USER_CONSUMPTION
ORDER BY DWH_CREATED_TIMESTAMP DESC
FETCH FIRST 20 ROWS ONLY;


The failure is not related to the migration process. The job fails because one of the table names coming from the source logs is longer than the column size defined in the target table. The column TABLE_PHYSICAL_NAME allows only 50 characters, but the incoming value is 53 characters, which causes Oracle to reject the insert.